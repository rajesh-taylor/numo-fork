<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_bg_scanner">

    <!-- Camera Preview -->
    <androidx.camera.view.PreviewView
        android:id="@+id/preview_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Scanner Overlay with Viewfinder -->
    <View
        android:id="@+id/scanner_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/scanner_overlay" />

    <!-- Top Bar with Close Button -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="48dp"
        android:paddingHorizontal="16dp">

        <ImageButton
            android:id="@+id/top_close_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="start"
            android:background="@drawable/bg_circle_button_dark"
            android:contentDescription="@string/common_close"
            android:src="@drawable/ic_close"
            app:tint="@android:color/white" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/checkout_scanner_title"
            android:textAppearance="@style/Text.Title"
            android:textColor="@android:color/white" />

    </FrameLayout>

    <!-- Scanned Item Overlay (initially hidden) -->
    <LinearLayout
        android:id="@+id/scanned_item_overlay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="@drawable/bg_scanner_item_overlay"
        android:paddingTop="@dimen/margin_screen_horizontal"
        android:paddingBottom="32dp"
        android:visibility="gone"
        android:translationY="300dp"
        tools:visibility="visible"
        tools:translationY="0dp">

        <!-- Item Info -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="24dp"
            android:paddingBottom="@dimen/margin_screen_horizontal">

            <!-- Item Image -->
            <androidx.cardview.widget.CardView
                android:id="@+id/item_image_container"
                android:layout_width="56dp"
                android:layout_height="56dp"
                app:cardCornerRadius="12dp"
                app:cardElevation="0dp"
                app:cardBackgroundColor="@color/color_bg_secondary">

                <ImageView
                    android:id="@+id/item_image"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="@string/desc_item_image"
                    android:scaleType="centerCrop" />

                <ImageView
                    android:id="@+id/item_image_placeholder"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:contentDescription="@string/item_entry_image_placeholder"
            android:src="@drawable/ic_image_placeholder"
            app:tint="@color/color_icon_tertiary" />
            </androidx.cardview.widget.CardView>

            <!-- Item Details -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="16dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/item_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textAppearance="@style/Text.RowTitle"
                    tools:text="Classic T-Shirt" />

                <TextView
                    android:id="@+id/item_variation"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textAppearance="@style/Text.RowSubtitle"
                    android:visibility="gone"
                    tools:text="Medium / Navy Blue"
                    tools:visibility="visible" />

                <TextView
                    android:id="@+id/item_price"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:textAppearance="@style/Text.Body"
                    tools:text="$24.99" />

            </LinearLayout>

            <!-- Quantity Controls -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageButton
                    android:id="@+id/decrease_button"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:padding="@dimen/space_m"
                    android:background="@drawable/bg_quantity_button"
                    android:src="@drawable/ic_minus"
                    app:tint="@color/color_text_primary"
                    android:scaleType="fitCenter"
                    android:contentDescription="@string/desc_decrease_quantity" />

                <TextView
                    android:id="@+id/item_quantity"
                    android:layout_width="40dp"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:text="1"
                    android:textAppearance="@style/Text.Subtitle" />

                <ImageButton
                    android:id="@+id/increase_button"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:padding="@dimen/space_m"
                    android:background="@drawable/bg_quantity_button"
                    android:src="@drawable/ic_plus"
                    app:tint="@color/color_text_primary"
                    android:scaleType="fitCenter"
                    android:contentDescription="@string/desc_increase_quantity" />

            </LinearLayout>

        </LinearLayout>

        <!-- Close Button -->
        <Button
            android:id="@+id/close_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginHorizontal="24dp"
            android:background="@drawable/bg_close_button_scanner"
            android:text="@string/checkout_scanner_done_button"
            android:textAllCaps="false"
            android:textAppearance="@style/Text.Button"
            android:textColor="@android:color/white" />

    </LinearLayout>

    <!-- Instruction Text (visible when no item scanned) -->
    <TextView
        android:id="@+id/instruction_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="120dp"
        android:text="@string/checkout_scanner_instruction_scan_to_add"
        android:textAppearance="@style/Text.Body"
        android:textColor="@color/color_text_on_dark_high" />

</FrameLayout>
