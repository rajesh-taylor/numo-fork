<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/space_m"
    app:cardBackgroundColor="@color/color_bg_white"
    app:cardCornerRadius="@dimen/radius_card"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Main Content (clickable to load basket) -->
        <LinearLayout
            android:id="@+id/basket_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="@dimen/space_l"
            android:paddingVertical="@dimen/row_padding_vertical"
            android:background="?attr/selectableItemBackground">

            <!-- Left: Icon -->
            <FrameLayout
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="@drawable/bg_basket_icon_circle">

                <ImageView
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_shopping_bag"
                    app:tint="@color/color_text_primary"
                    android:contentDescription="@null" />

            </FrameLayout>

            <!-- Middle: Info -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="@dimen/row_icon_gap"
                android:layout_marginEnd="@dimen/space_m"
                android:orientation="vertical">

                <!-- Name -->
                <TextView
                    android:id="@+id/basket_name"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textAppearance="@style/Text.RowTitle"
                    android:maxLines="1"
                    android:ellipsize="end"
                    tools:text="Table 5" />

                <!-- Items summary -->
                <TextView
                    android:id="@+id/basket_summary"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:textAppearance="@style/Text.RowSubtitle"
                    android:textColor="@color/color_text_secondary"
                    android:maxLines="1"
                    android:ellipsize="end"
                    tools:text="4 × Coffee, 2 × Pizza + 2 more" />

                <!-- Item count & time -->
                <TextView
                    android:id="@+id/basket_meta"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_xs"
                    android:textAppearance="@style/Text.Caption"
                    tools:text="6 items • Updated 5 min ago" />

            </LinearLayout>

            <!-- Right: Total -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="end">

                <TextView
                    android:id="@+id/basket_total"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textAppearance="@style/Text.RowTitle"
                    tools:text="$45.99" />

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_marginTop="@dimen/space_xs"
                    android:layout_gravity="end"
                    android:src="@drawable/ic_chevron_right"
                    app:tint="@color/color_text_tertiary"
                    android:contentDescription="@null" />

            </LinearLayout>

        </LinearLayout>

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginHorizontal="@dimen/space_l"
            android:background="@color/color_divider" />

        <!-- Actions Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="@dimen/space_s">

            <!-- Edit Name Button -->
            <TextView
                android:id="@+id/edit_name_button"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:gravity="center"
                android:text="@string/saved_baskets_action_rename"
                android:textAppearance="@style/Text.SecondaryButton"
                android:textColor="@color/color_accent_blue"
                android:background="?attr/selectableItemBackground" />

            <!-- Vertical Divider -->
            <View
                android:layout_width="1dp"
                android:layout_height="24dp"
                android:background="@color/color_divider" />

            <!-- Delete Button -->
            <TextView
                android:id="@+id/delete_button"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:gravity="center"
                android:text="@string/saved_baskets_action_delete"
                android:textAppearance="@style/Text.SecondaryButton"
                android:textColor="@color/color_warning_red"
                android:background="?attr/selectableItemBackground" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
